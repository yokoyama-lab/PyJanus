// @summary:    fewer arguments than the format string consumes
// @error-kind: execution
// @error:      Not enough arguments for format string

procedure main()
  int x
  int y
  x += 42
  y += 1337

  // Should return argument mismatch
  printf("The answer to life, the universe and everything is %d %d", x)
