// @summary:    swapping an array cell with one whose index reads the same array
// @error-kind: execution
// @error:      Identifiers `a' and `a' are aliases

procedure main()
  int a[2] = {1,2}

  a[0] <=> a[a[0]]  // a[0] on the both hand sides.
  a[0] <=> a[a[0]]
