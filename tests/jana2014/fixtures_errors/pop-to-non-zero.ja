// @summary:    popping into a variable that is not zero
// @error-kind: execution
// @error:      Can't pop to non-zero variable `x'

procedure main()
    int x
    stack s
    x += 5
    push(x, s)
    x += 3
    pop(x, s)
